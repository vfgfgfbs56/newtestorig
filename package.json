{
  "name": "secure-qr-auth-worker",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "npm run build && wrangler dev",
    "dev:ui": "vite",
    "deploy": "npm run build && wrangler deploy",
    "check": "node --check worker/index.js && node --check worker/auth.js && node --check worker/schema.js && node --check src/app.js && node --check src/vault.js"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1",
    "qrcode-generator": "2.0.4"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "wrangler": "4.129.0"
  }
}
