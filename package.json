{
  "name": "secure-qr-auth-worker",
  "version": "0.8.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "npm run build && wrangler dev",
    "dev:ui": "vite",
    "deploy": "npm run build && wrangler deploy",
    "check": "node --check worker/index.js && node --check worker/auth.js && node --check worker/schema.js && node --check worker/jend.js && node --check worker/security.js && node --check src/app.js && node --check src/vault.js && node --check src/jend.js && node --check src/security.js",
    "jend:keys": "node scripts/generate-jend-keys.mjs",
    "guard:build": "bash scripts/build-guard.sh"
  },
  "dependencies": {
    "@zxing/browser": "0.2.1",
    "qrcode-generator": "2.0.4"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "wrangler": "4.129.0"
  }
}
